node_modules/
build/
dist/
.wrangler/
.dev.vars
.dev.vars.*
.env
.env.*
*.log
.DS_Store
.test-executor/
.test-executor-e2e/
.test-meth/
.test-*/
test-e2e.mjs
test-methodology.mjs
docs/progress.md

# CRITICAL: the engine contains all proprietary methodology and server-side logic.
# It must NEVER be pushed to a public repo. Keep it local (and on Cloudflare).
engine/

# CRITICAL: admin scripts contain the admin token (Worker secret).
# Never commit them to public.
admin/

